@using ATI.Web.Areas.Core.Models.Layout
@using ATI.Web.Areas.Core.Views.Shared.Components.CoreLogo
@model HeaderViewModel
@{
    Layout = null;
    var theme = await GetTheme();
}
<a href="@Url.Action("Index", "Home", new {area = string.Empty})">
    @await Component.InvokeAsync(typeof(CoreLogoViewComponent), new {LogoSkin = theme.BaseSettings.Layout.DarkMode ? "dark": "light", logoClass = "d-none d-lg-inline h-30px"})
    @await Component.InvokeAsync(typeof(CoreLogoViewComponent), new {LogoSkin = theme.BaseSettings.Layout.DarkMode ? "dark-sm": "light-sm", logoClass = "d-lg-none h-25px"})
</a>
